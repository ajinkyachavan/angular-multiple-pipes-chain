import { AsyncObsFilterPipe } from './async-obs-filter.pipe';

describe('AsyncObsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncObsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
